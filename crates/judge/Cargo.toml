[package]
name = "codexec-judge"
description = "Execution engine: sandbox abstraction, isolate backend, language registry and judging pipeline"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[features]
# Exposes `codexec_judge::mock` so other crates can test against a scripted sandbox.
mock = []

[dependencies]
async-trait.workspace = true
serde.workspace = true
sha2.workspace = true
thiserror.workspace = true
tokio.workspace = true
toml.workspace = true
tracing.workspace = true

[dev-dependencies]
serde_json.workspace = true
