# A + B

Read two integers `a` and `b` from standard input and print their sum.

## Input

One line with two integers separated by a space, `-10^18 <= a, b <= 10^18`.

## Output

One line with `a + b`.

## Example

Input:

```
2 40
```

Output:

```
42
```
